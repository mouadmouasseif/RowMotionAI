"use client";
import { use, useEffect, useState } from "react";
import Link from "next/link";
import { ArrowLeft, FileVideo, Users } from "lucide-react";
import { AppShell } from "@/components/AppShell"; import { ProtectedPage } from "@/components/ProtectedPage"; import { useAuth } from "@/providers/AuthProvider"; import { listCoaches, listAthletes } from "@/services/user-service"; import { listAnalyses } from "@/services/analysis-service"; import { displayAge } from "@/lib/user-profile"; import type { UserProfile } from "@/types/user";
function CoachProfile({ coachId }: { coachId: string }) { const { profile } = useAuth(); const [coach, setCoach] = useState<UserProfile | null>(null); const [athleteCount, setAthleteCount] = useState(0); const [analysisCount, setAnalysisCount] = useState(0); useEffect(() => { if (!profile) return; void Promise.all([listCoaches(profile), listAthletes(profile), listAnalyses(profile)]).then(([coaches, athletes, analyses]) => { setCoach(coaches.find((item) => item.uid === coachId) ?? null); setAthleteCount(athletes.filter((item) => item.coachId === coachId).length); setAnalysisCount(analyses.filter((item) => item.coachId === coachId || item.createdBy === coachId).length); }); }, [coachId, profile]); if (!profile) return null; return <AppShell title="Profil entraîneur" subtitle="Gestion du compte"><Link className="back-link" href="/coaches"><ArrowLeft />Retour aux entraîneurs</Link>{!coach ? <div className="empty-state"><p>Profil coach introuvable.</p></div> : <><section className="profile-summary"><div className="profile-avatar large">{coach.firstName[0]}{coach.lastName[0]}</div><div><h2>{coach.firstName} {coach.lastName}</h2><p>{coach.email} · {coach.phone ?? "Téléphone non renseigné"}</p><small>{coach.specialty ?? "Spécialité non renseignée"} · Licence {coach.licenseNumber ?? "—"} · {displayAge(coach) ? `${displayAge(coach)} ans` : "Âge non renseigné"}</small></div><span className={`account-status ${coach.active ? "active" : "pending"}`}>{coach.active ? "Actif" : "En attente"}</span></section><div className="dash-stats"><article><span><Users /></span><div><small>Athlètes suivis</small><strong>{athleteCount}</strong></div></article><article><span><FileVideo /></span><div><small>Analyses</small><strong>{analysisCount}</strong></div></article><article><span><FileVideo /></span><div><small>Club</small><strong>{coach.clubId ? "Associé" : "—"}</strong></div></article></div></>}</AppShell>; }
export default function CoachProfilePage({ params }: { params: Promise<{ coachId: string }> }) { const { coachId } = use(params); return <ProtectedPage allowedRoles={["club_admin", "superadmin"]}><CoachProfile coachId={coachId} /></ProtectedPage>; }
