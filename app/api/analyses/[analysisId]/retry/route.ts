import { apiError,requireApiUser } from "@/lib/api-auth"; import { authorizedAnalysis,createJob } from "@/lib/analysis-api";
export async function POST(request:Request,{params}:{params:Promise<{analysisId:string}>}){try{const user=await requireApiUser(request);const {analysisId}=await params;const {data}=await authorizedAnalysis(analysisId,user);if(data.status!=="failed"&&data.status!=="cancelled")throw new Error("INVALID_STATUS");await createJob(analysisId,user);return Response.json({success:true,data:{analysisId,status:"queued"}},{status:202});}catch(reason){return apiError(reason);}}
