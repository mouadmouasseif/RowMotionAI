# Tests

Frontend : `npm run typecheck`, `npm run lint`, `npm run test`, `npm run build`. Backend, depuis `services/biomechanics-api` : `pytest`. Les tests Emulator et Playwright restent à écrire; la commande E2E ne prétend pas les exécuter.
